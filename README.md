# SyteLine Reports

This repository hosts a FastAPI-based reporting portal for SyteLine (Infor CloudSuite Industrial) that allows generating sales summaries and ad-hoc queries via the REST/OData API.
